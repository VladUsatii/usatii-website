import QrCodeDesigner from "./qr-code-designer";

export const metadata = {
  title: "QR Code Designer | USATII",
  description: "Create downloadable QR codes with USATII's first-party QR tool.",
};

export default function QrCodePage() {
  return <QrCodeDesigner />;
}

